spring:
  application:
    name: kafka-demo-2
  kafka:
    bootstrap-servers: 192.168.215.128:9092
    listener:
      ack-mode: manual

# Custom configuration
kafka:
  topic:
    name: helloTopic
  consumer:
    group: helloGroup